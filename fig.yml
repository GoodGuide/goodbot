transactor:
  build: ./transactor
  ports: 
    - "4334:4334"
    - "4335:4335"
  volumes:
    - ./data:/app/data

bot:
  build: .
  command: lein run
  links:
    - transactor
  environment:
    - GOODBOT_CHANNELS=#goodbot-test
    - GOODBOT_NICK=goodbot-test
